import React, {useState, useEffect} from 'react';

const About = props => {
    const initialState =  {

    }

    const [state, setState] = useState(initialState);

    return (
        <div>

        </div>
    );
}
